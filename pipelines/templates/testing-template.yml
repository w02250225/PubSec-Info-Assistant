# Copyright (c) Microsoft Corporation.
# Licensed under the MIT license.

# Template for Functional Tests. Modify and add steps here.

parameters:
- name: fullBuild  
  type: boolean
  default: true
- name: environment
  type: string
  default: ''

steps:
  - script: echo The value of environment is ${{ parameters.environment }}
    displayName: Check environment value
  - ${{ if startsWith(parameters.environment, 'tmp') }}:
    - template: make-command.yml
      parameters:
        displayName: 'Functional Tests'
        makeCommand: 'functional-tests' 

 
  